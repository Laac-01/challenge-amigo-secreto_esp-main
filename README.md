Juego amigo secreto

Funcionalaidades:

    1. `agregarAmigo()`
        -Está funcion toma los parametros de la caja de texto y los gusrda en un arreglo.
    2.`sorteoAmigo()`
        -Esta funcion realiza el sorteo del amigo secreto mediante en sorteo aleatorio, tomando el tamaño del arreglo arregloAmogos y multiplicando esto por la funcon Math.ramdon()
    3.`listarAmigos()`
        -Está funcion imprime en pantalla los nombres que se van agreagando.

